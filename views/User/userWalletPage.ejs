<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChapterOne | Wallet</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&family=Playfair+Display:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-brown: #8B5A2B;
            --secondary-brown: #A67C52;
            --light-brown: #D2B48C;
            --dark-brown: #654321;
            --accent-gold: #D4AF37;
            --soft-shadow: rgba(0, 0, 0, 0.05);
            --card-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
            --sidebar-width: 260px;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
        }
        .back-home-btn {
            color: var(--primary-brown);
            text-decoration: none;
            display: flex;
            align-items: center;
            font-size: 14px;
            padding: 6px 12px;
            border: 1px solid var(--primary-brown);
            border-radius: 4px;
            transition: all 0.3s ease;
        }
        .back-home-btn:hover {
            background-color: var(--primary-brown);
            color: white;
        }

        .dashboard-container {
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar Styles */
        .dashboard-sidebar {
            width: var(--sidebar-width);
            background-color: white;
            position: fixed;
            height: 100vh;
            box-shadow: var(--card-shadow);
            padding: 25px 0;
            overflow-y: auto;
            z-index: 10;
            transition: transform 0.3s ease;
        }

        
            .logo-text {
            font-family: 'Playfair Display', serif;
            color: var(--primary-brown);
            margin: 0;
        }
        

        .user-profile .profile-image img {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border: 3px solid var(--light-brown);
        }

        .sidebar-nav ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .sidebar-nav li {
            margin-bottom: 5px;
        }

        .nav-link1 {
            display: flex;
            align-items: center;
            padding: 12px 20px;
            color: #555;
            text-decoration: none;
            transition: all 0.3s ease;
            border-left: 3px solid transparent;
        }

        .nav-link1.active {
            background-color: rgba(139, 90, 43, 0.1);
            color: var(--primary-brown);
            border-left-color: var(--primary-brown);
        }

        .nav-link1:hover {
            background-color: rgba(139, 90, 43, 0.1);
            color: var(--primary-brown);
            border-left-color: var(--primary-brown);
        }

        .nav-link1 i {
            margin-right: 12px;
            width: 20px;
            text-align: center;
        }

        /* Main Content Styles */
        .dashboard-content {
            flex: 1;
            margin-left: var(--sidebar-width);
            padding: 30px;
            transition: margin-left 0.3s ease;
        }

        .dashboard-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
        }

        .dashboard-header h2 {
            font-family: 'Playfair Display', serif;
            color: var(--primary-brown);
            margin: 0;
        }

        /* Wallet Cards */
        .wallet-summary-card {
            background-color: white;
            border-radius: 10px;
            padding: 25px;
            margin-bottom: 25px;
            box-shadow: var(--card-shadow);
            background-image: linear-gradient(to right, rgba(255,255,255,0.95), rgba(255,255,255,0.9)), url('/images/pattern-bg.jpg');
            background-size: cover;
        }

        .wallet-amount {
            font-size: 2.5rem;
            font-weight: 600;
            color: var(--primary-brown);
        }

        .primary-btn {
            background-color: var(--primary-brown);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .primary-btn:hover {
            background-color: var(--dark-brown);
            color: white;
        }

        .outline-btn {
            background-color: transparent;
            color: var(--primary-brown);
            border: 1px solid var(--primary-brown);
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .outline-btn:hover {
            background-color: rgba(139, 90, 43, 0.1);
        }

        /* Content Cards */
        .content-card {
            background-color: white;
            border-radius: 10px;
            box-shadow: var(--card-shadow);
            margin-bottom: 25px;
            overflow: hidden;
        }

        .card-header {
            padding: 15px 20px;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .card-header h5 {
            margin: 0;
            color: var(--secondary-brown);
            font-family: 'Playfair Display', serif;
        }

        .card-body {
            padding: 20px;
        }

        /* Table Styles */
        .custom-table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0 5px;
        }

        .custom-table th {
            color: #888;
            font-weight: 500;
            padding: 12px 15px;
            border: none;
        }

        .custom-table td {
            padding: 12px 15px;
            vertical-align: middle;
            border-top: 1px solid #f0f0f0;
            border-bottom: 1px solid #f0f0f0;
        }

        .transaction-badge {
            padding: 5px 10px;
            border-radius: 50px;
            font-size: 12px;
            font-weight: 500;
        }

        .transaction-badge.credit {
            background-color: rgba(39, 174, 96, 0.1);
            color: #27ae60;
        }

        .transaction-badge.debit {
            background-color: rgba(231, 76, 60, 0.1);
            color: #e74c3c;
        }

        .transaction-badge.pending {
            background-color: rgba(241, 196, 15, 0.1);
            color: #f1c40f;
        }

        .amount-credit {
            color: #27ae60;
        }

        .amount-debit {
            color: #e74c3c;
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 20px;
        }

        .empty-icon {
            font-size: 36px;
            color: #ddd;
            margin-bottom: 10px;
        }

        .empty-state p {
            color: #888;
            margin-bottom: 15px;
        }

        /* Toggle Button */
        .toggle-sidebar {
            display: none;
            position: fixed;
            top: 15px;
            left: 15px;
            z-index: 100;
            background-color: var(--primary-brown);
            color: white;
            border: none;
            width: 40px;
            height: 40px;
            border-radius: 5px;
            align-items: center;
            justify-content: center;
            cursor: pointer;
        }

        /* Form styles */
        .form-control {
            border: 1px solid #e0e0e0;
            padding: 12px 15px;
        }

        .form-control:focus {
            border-color: var(--light-brown);
            box-shadow: 0 0 0 0.2rem rgba(139, 90, 43, 0.25);
        }

        /* Responsive */
        @media (max-width: 992px) {
            .dashboard-sidebar {
                transform: translateX(-100%);
            }
            
            .dashboard-sidebar.active {
                transform: translateX(0);
            }
            
            .dashboard-content {
                margin-left: 0;
            }
            
            .toggle-sidebar {
                display: flex;
            }
        }
        .logo-container {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 20px;
            margin-bottom: 25px;
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <!-- Mobile Toggle Button (visible on small screens) -->
        <button class="toggle-sidebar d-lg-none">
            <i class="fas fa-bars"></i>
        </button>
        
        <!-- Sidebar -->
        <div class="dashboard-sidebar">

            <div class="logo-container">
                <a href="/" class="back-home-btn">
                    <i class="fas fa-home me-1"></i> Back To Home
                </a>
            </div>

            <div class="logo-container">
                <h3 class="logo-text">ChapterOne</h3>
            </div>
            
            <div class="user-profile text-center mb-4">
                <div class="profile-image">
                    <img src="<%= user.profileImage %>" alt="Profile Picture" class="rounded-circle">
                </div>
                <h5 class="mt-3"><%= user.name %></h5>
                <p class="text-muted small"><%= user.email %></p>
            </div>
            
            <nav class="sidebar-nav">
                <ul>
                    <li>
                        <a href="/account" class="nav-link1">
                            <i class="fas fa-th-large"></i>
                            <span>Dashboard</span>
                        </a>
                    </li>
                    <li>
                        <a href="/account/profile" class="nav-link1">
                            <i class="fas fa-user"></i>
                            <span>Profile</span>
                        </a>
                    </li>
                    <li>
                        <a href="/account/edit-profile" class="nav-link1">
                            <i class="fas fa-user-edit"></i>
                            <span>Edit Profile</span>
                        </a>
                    </li>
                    <li>
                        <% if (!user.googleId) { %>
                            <a href="/account/change-password" class="nav-link1">
                                <i class="fas fa-lock"></i>
                                <span>Change Password</span>
                            </a>
                            <% } %>
                    </li>
                    <!-- <li>
                        <% if (!user.googleId) { %>
                            <a href="/account/change-email" class="nav-link1">
                                <i class="fas fa-envelope"></i>
                                <span>Change Email</span>
                            </a>
                            <% } %>
                    </li> -->
                    <li>
                        <a href="/account/orders" class="nav-link1">
                            <i class="fas fa-shopping-bag"></i>
                            <span>Orders</span>
                        </a>
                    </li>
                    <li>
                        <a href="/account/wallet" class="nav-link1 active">
                            <i class="fas fa-wallet"></i>
                            <span>Wallet</span>
                        </a>
                    </li>
                    <li class="mt-4">
                        <a href="/logout" class="nav-link1 text-danger">
                            <i class="fas fa-sign-out-alt"></i>
                            <span>Logout</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </div>
        
        <!-- Main Content -->
        <div class="dashboard-content">
            <div class="dashboard-header">
                <h2>Wallet</h2>
            </div>
            
            <!-- Wallet Summary Card -->
            <div class="wallet-summary-card">
                <div class="row align-items-center">
                    <div class="col-md-8">
                        <p class="text-muted mb-1">Current Balance</p>
                        <div class="wallet-amount mb-3">₹<%= userWallet && userWallet.amount ? userWallet.amount : '0.00' %></div>
                    </div>
                    <div class="col-md-4 text-md-end">
                        <div class="wallet-icon">
                            <i class="fas fa-wallet fa-4x text-muted opacity-25"></i>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Wallet Features -->
            <!-- <div class="row mb-4">
                <div class="col-md-4 mb-3">
                    <div class="content-card h-100">
                        <div class="card-body text-center p-4">
                            <div class="feature-icon mb-3">
                                <i class="fas fa-gift fa-2x text-primary"></i>
                            </div>
                            <h5 class="mb-2">Referral Bonus</h5>
                            <p class="mb-0">Invite friends and earn ₹100 for each successful referral.</p>
                            <div class="mt-3">
                                <a href="#referralModal" data-bs-toggle="modal" class="btn outline-btn btn-sm">Invite Now</a>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 mb-3">
                    <div class="content-card h-100">
                        <div class="card-body text-center p-4">
                            <div class="feature-icon mb-3">
                                <i class="fas fa-percent fa-2x text-primary"></i>
                            </div>
                            <h5 class="mb-2">Cashback</h5>
                            <p class="mb-0">Get up to 5% cashback on every purchase when using wallet balance.</p>
                            <div class="mt-3">
                                <a href="/shop" class="btn outline-btn btn-sm">Shop Now</a>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 mb-3">
                    <div class="content-card h-100">
                        <div class="card-body text-center p-4">
                            <div class="feature-icon mb-3">
                                <i class="fas fa-tag fa-2x text-primary"></i>
                            </div>
                            <h5 class="mb-2">Exclusive Offers</h5>
                            <p class="mb-0">Unlock special deals and discounts with your wallet balance.</p>
                            <div class="mt-3">
                                <a href="/offers" class="btn outline-btn btn-sm">View Offers</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div> -->
           
            <!-- Transaction History -->
            <!-- <div class="content-card">
                <div class="card-header">
                    <h5>Transaction History</h5>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table custom-table">
                            <thead>
                                <tr>
                                    <th>Transaction ID</th>
                                    <th>Date</th>
                                    <th>Description</th>
                                    <th>Type</th>
                                    <th>Amount</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (transactions && transactions.length > 0) { %>
                                <% transactions.forEach(transaction => { %>
                                <tr>
                                    <td>#<%= transaction._id.toString().slice(-8) %></td>
                                    <td><%= new Date(transaction.createdAt).toLocaleDateString('en-IN', { day: '2-digit', month: 'short', year: 'numeric' }) %></td>
                                    <td><%= transaction.description %></td>
                                    <td>
                                        <span class="transaction-badge <%= transaction.type.toLowerCase() %>">
                                            <%= transaction.type %>
                                        </span>
                                    </td>
                                    <td class="<%= transaction.type === 'Credit' ? 'amount-credit' : 'amount-debit' %>">
                                        <%= transaction.type === 'Credit' ? '+' : '-' %>₹<%= transaction.amount.toFixed(2) %>
                                    </td>
                                </tr>
                                <% }) %>
                                <% } else { %>
                                <tr>
                                    <td colspan="5">
                                        <div class="empty-state">
                                            <div class="empty-icon">
                                                <i class="fas fa-receipt"></i>
                                            </div>
                                            <p>No transactions found in your wallet history.</p>
                                            <a href="#addFundsModal" data-bs-toggle="modal" class="btn primary-btn btn-sm">Add Funds</a>
                                        </div>
                                    </td>
                                </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div> -->
        </div>
    </div>




    <!-- Referral Modal -->
    <div class="modal fade" id="referralModal" tabindex="-1" aria-labelledby="referralModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="referralModalLabel">Refer & Earn</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body text-center">
                    <i class="fas fa-gift fa-3x text-primary mb-3"></i>
                    <h5 class="mb-3">Invite friends and earn ₹100</h5>
                    <p>Share your referral code with friends. When they sign up and make their first purchase, you both get ₹100 in your wallet!</p>
                    <div class="input-group mb-3">
                        <input type="text" class="form-control" value="<%= user.referralCode || 'BOOKWORM123' %>" id="referralCode" readonly>
                        <button class="btn primary-btn" type="button" onclick="copyReferralCode()">
                            <i class="fas fa-copy"></i>
                        </button>
                    </div>
                    <div class="d-grid gap-2">
                        <a href="#" class="btn outline-btn" onclick="shareReferral()">
                            <i class="fas fa-share-alt me-2"></i> Share
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS and Popper.js -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    <script>
        // Toggle sidebar on mobile
        document.querySelector('.toggle-sidebar')?.addEventListener('click', function() {
            document.querySelector('.dashboard-sidebar').classList.toggle('active');
        });

        // Close sidebar when clicking outside on mobile
        document.addEventListener('click', function(event) {
            const sidebar = document.querySelector('.dashboard-sidebar');
            const toggleBtn = document.querySelector('.toggle-sidebar');
            
            if (window.innerWidth <= 992 && 
                sidebar.classList.contains('active') && 
                !sidebar.contains(event.target) && 
                event.target !== toggleBtn) {
                sidebar.classList.remove('active');
            }
        });

        // Copy referral code
        function copyReferralCode() {
            const referralCodeInput = document.getElementById('referralCode');
            referralCodeInput.select();
            document.execCommand('copy');
            alert('Referral code copied to clipboard!');
        }

        // Share referral code
        function shareReferral() {
            if (navigator.share) {
                navigator.share({
                    title: 'Join ChapterOne Bookstore',
                    text: 'Use my referral code ' + document.getElementById('referralCode').value + ' to get ₹100 on signup!',
                    url: window.location.origin
                })
                .catch(error => console.log('Error sharing:', error));
            } else {
                alert('Share feature not supported in your browser. Please copy the referral code manually.');
            }
        }
    </script>
</body>
</html>